sum :: fn(x: i64, y: i64) -> i64
{
	return x + y
}

mul :: fn(x: i64, y: i64) -> i64
{
	return x * y
}

main :: fn -> i64
{
	x := 12

	if x > 5
	{
		y := true

		if y
		{
			x = 42
		}
	}
	else
	{
		x = sum(x, 12)
	}

	return x
}
