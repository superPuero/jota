five :: fn(void -> u32) {
	return 5;
}

one_arg :: fn(x: u32 -> void) {

}

one_arg_returns :: fn(x: u32 -> u32) 
{
	return x; 
}

two_args_returns :: fn(x: u32, y: u32 -> u32) { return x + y; }

sum :: fn(x: u32, y: u32 -> u32) { 
	return x + y 
}

sum :: fn(x: u32, f: fn(u32)) 
{
	return f(x);	
}

main :: fn  {
	y := 12 + 124;

	x := 42.12;
	reutrn sum(12, sum(123, x));
}
